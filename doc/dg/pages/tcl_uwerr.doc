/** \page tcl_uwerr The uwerr comand
    \verbatim uwerr <data> <nrep> {<col>|<f>} [<s_tau> [<f_args>]] [plot]\endverbatim
    calculates the value, the error and the error of the error for arbitrary functions of
    elementary observables in Monte Carlo simulations.

    <dl>
    <dt>data</dt><dd>is a matrix filled with the primary estimates \f$a_\alpha^{i,r}\f$ from \f$R\/\f$
                replica with \f$N_1,N_2,\ldots,N_R\f$ measurements each.<br>
                <center>\f[data=\left(\begin{array}{*{4}{c}}
		             a_1^{1,1}&a_2^{1,1}&a_3^{1,1}&\cdots\\
		             a_1^{2,1}&a_2^{2,1}&a_3^{2,1}&\cdots\\
			     \vdots&\vdots&\vdots&\vdots\\
		             a_1^{{N_1},1}&a_2^{{N_1},1}&a_3^{{N_1},1}&\cdots\\
		             a_1^{1,2}&a_2^{1,2}&a_3^{1,2}&\cdots\\
			     \vdots&\vdots&\vdots&\vdots\\
		             a_1^{{N_R},R}&a_2^{{N_R},R}&a_3^{{N_R},R}&\cdots\\
		             \end{array}\right)\f]</center></dd>
    <dt>nrep</dt><dd>is a vector whose elements specify the length of the individual replica.<br>
                <center>\f$$nrep=\left(N_1,N_2,\ldots,N_R\right)$\f$</center></dd>
    <dt>col</dt><dd>is the column of the data which is to be analyzed. I.e. \f$1\le\textrm{col}\le\textrm{number of columns}\f$.</dd>
    <dt>f</dt><dd>is a user defined tcl function returning a double with first argument a vector which
             has as many entries as data has columns. If \em f\/ is given instead of the column the
	     coresponding derived quantity is anlayzed.</dd>
    <dt>f_args</dt><dd>further arguments to \em f.</dd>
    <dt>s_tau</dt><dd>is the estimate \f$S=\tau/tau_{\textrm{int}}\f$ as explained in section (3.3) of
                 [1]. The default is 1.5 and it is never taken larger than \f$\min_{r=1}^R{N_r}/2\f$.</dd>
    <dt>'plot'</dt><dd>If plot is given you will get the plots of \f$\Gamma/\Gamma(0)\f$ and \f$\tau_{int}\f$ vs. \f$W\f$.<br>The data and gnuplot script is written to the current directory.</dd>
    </dl>
    <br>
    It returns a string containing the following values in the given order.
            <ol>
                <li>The calculated value
                <li>The error
                <li>The error of the error
                <li>The integrated autocorrelation time
                <li>The error of that
		<li>if length(nrep) > 1 The Q-value, i.e. the probability to find a \f$\chi^2\f$ fit of the replica estimates
            </ol>

    The users is warned with a string if the windowing failed or if the error in one of the replica is to large.

    This function is based on the matlab function presented in<br>
    [1] arXiv:hep-lat/0306017 v1 13. Jun 2003, Wolff, U., <a href="http://arxiv.org/pdf/hep-lat/0306017">Monte Carlo errors with less errors.</a>
*/
