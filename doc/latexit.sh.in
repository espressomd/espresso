#!/bin/sh
SRCDIR=$1
TEXFILE=$2
TEXINPUTS=.:$SRCDIR:@abs_srcdir@:
BIBINPUTS=.:$SRCDIR:
export TEXINPUTS BIBINPUTS
@PDFLATEX@ $TEXFILE || exit 1
@MAKEINDEX@ $TEXFILE || exit 1
@BIBTEX@ $TEXFILE || exit 1
@PDFLATEX@ $TEXFILE || exit 1
@PDFLATEX@ $TEXFILE || exit 1
